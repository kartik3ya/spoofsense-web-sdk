{
  "name": "spoofsense-web-sdk-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "SpoofSense Web SDK — selfie capture with liveness + deepfake verification and injection-attack protection.",
  "workspaces": [
    "packages/core",
    "packages/react"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/core && npm run build --workspace packages/react",
    "typecheck": "npm run typecheck --workspaces",
    "example": "node examples/vanilla/serve.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
